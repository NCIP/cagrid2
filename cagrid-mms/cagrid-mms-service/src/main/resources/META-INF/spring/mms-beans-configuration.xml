<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:p="http://www.springframework.org/schema/p"
	xmlns:tx="http://www.springframework.org/schema/tx"
	xsi:schemaLocation="
	        http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-2.5.xsd
	        http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-2.5.xsd">

	<bean id="defaultPropertiesPlaceholder"
		class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
		<property name="location" value="classpath:META-INF/spring/mms.properties" />
		<property name="ignoreResourceNotFound" value="true" />
		<property name="ignoreUnresolvablePlaceholders" value="true" />
	</bean>

	<bean id="jaxbResourceProperties" class="java.util.HashMap">
		<constructor-arg>
			<map>
				<entry key="serviceMetadata" value="${cagrid.gme.service.metadata.url}" />
				<entry key="serviceSecurityMetadata" value="${cagrid.gme.service.security.metadata.url}" />
			</map>
		</constructor-arg>
	</bean>


	<!-- This specifies the caDSR-based implementation -->
	<bean id="mms" class="org.cagrid.mms.service.impl.cadsr.CaDSRMMSImpl">
		<!-- Used to specify the MMS metadata constructed below -->
		<constructor-arg>
			<ref bean="metadata" />
		</constructor-arg>
		<!-- These keys represent the sourceIDs that are supported, and the caDSR 
			ApplicationService remote URL which will be used for the corresponding sourceID -->
		<constructor-arg>
			<map>
				<entry key="caDSR Production" value="http://cadsrapi.nci.nih.gov/cadsrapi40/" />
			</map>
		</constructor-arg>
	</bean>


	<!-- The metadata defines the default source, and the list of supported 
		sources -->
	<bean id="metadata" class="org.cagrid.mms.model.ModelSourceMetadata">
		<property name="defaultSourceIdentifier" value="caDSR Production" />
		<property name="supportedModelSources" ref="supportedModelSources" />
	</bean>

	<bean id="supportedModelSources"
		class="org.cagrid.mms.model.ModelSourceMetadata.SupportedModelSources">
		<lookup-method name="getSource" bean="sources" />
	</bean>

	<bean id="sources" class="java.util.ArrayList">
		<constructor-arg>
			<list>
				<ref bean="caDSRProductionSource" />
			</list>
		</constructor-arg>
	</bean>

	<!-- The caDSR based-implemention assumes the supported source properties 
		(and that they are the same for all sources) , and will populate them automatically; 
		they don't need to be specified for the listed sources -->
	<bean id="caDSRProductionSource" class="org.cagrid.mms.model.SourceDescriptor">
		<property name="identifier" value="caDSR Production" />
		<property name="description"
			value="The production instance of the National Cancer Institute's Cancer Data Standards Repository (caDSR)." />
	</bean>

	<bean id="mmsService" class="org.cagrid.mms.service.impl.MetadataModelServiceImpl">
		<constructor-arg ref="mms" />
		<constructor-arg ref="jaxbResourceProperties" />
	</bean>
</beans>