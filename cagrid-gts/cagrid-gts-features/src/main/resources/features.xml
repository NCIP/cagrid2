<?xml version="1.0"?>
<features>
	<feature name="cagrid-gts" version="${project.version}">
		<bundle>mvn:${project.groupId}/cagrid-gts-api/${project.version}
		</bundle>
		<bundle>mvn:${project.groupId}/cagrid-gts-service/${project.version}
		</bundle>
		<bundle>mvn:${project.groupId}/cagrid-gts-soap-client/${project.version}
		</bundle>
	</feature>
	<feature name="cagrid-gts-client" version="${project.version}">
		<bundle>mvn:${project.groupId}/cagrid-gts-api/${project.version}
		</bundle>
		<bundle>mvn:${project.groupId}/cagrid-gts-soap-client/${project.version}
		</bundle>
	</feature>
</features>